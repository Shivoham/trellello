import React, { Component } from 'react';

import ColumnsList from './columns-list.js';

export default class App extends Component {
  render() {
    return (
      <div>
        <ColumnsList />
      </div>
    );
  }
}
